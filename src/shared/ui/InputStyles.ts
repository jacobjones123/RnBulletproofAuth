import { StyleSheet } from 'react-native';

export const InputStyles = StyleSheet.create({
  wrap: { marginBottom: 12 },
  input: {
    borderWidth: 1,
    borderColor: '#E5E7EB',
    borderRadius: 8,
    padding: 12,
    fontSize: 16,
  },
});
